# Challenge 4 — Find the Largest Number

## Problem Statement

Write a JavaScript program that takes an array of integers and returns the largest number in the array.

Don't change the function name in the template when writing your solution. Don't hardcode for only the test cases.

## Input Format

An array of integers, `arr`.

## Output Format

A single integer representing the largest number in `arr`.

## Constraints

- `1 <= arr.length <= 1000`
- Each element is an integer.
- The array contains at least one element.

## Template

```js
function findLargest(arr) {
  // Change only this area
  return result;
}

module.exports = findLargest;

if (require.main === module) {
    require("../../index.js")(__filename);
}
```

## Example

**Input**
```text
[3, 7, 2, 9, 4]
```

**Output**
```text
9
```

**Explanation**

The largest number in the array is `9`.
