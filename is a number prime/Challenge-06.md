# Challenge 6 — Check if a Number is Prime

## Problem Statement

Write a JavaScript program that takes an integer and returns whether it is a prime number.

Don't change the function name in the template when writing your solution. Don't hardcode for only the test cases.

## Input Format

An integer, `num`.

## Output Format

A boolean — `true` if `num` is prime, `false` otherwise.

## Constraints

- `1 <= num <= 1000000`

## Template

```js
function isPrime(num) {
  // Change only this area
  return result;
}

module.exports = isPrime;

if (require.main === module) {
    require("../../index.js")(__filename);
}
```

## Example

**Input**
```text
7
```

**Output**
```text
true
```

**Explanation**

`7` is only divisible by `1` and itself, so it is a prime number.
